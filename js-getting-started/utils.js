// waiting for code...

//changes HERO offer on site
function showMessage(itemName, discount, price) {
    document.getElementById('item').textContent = itemName;
    document.getElementById('discount').textContent = discount;
    document.getElementById('price').textContent = price;
}